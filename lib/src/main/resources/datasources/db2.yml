connectorIds:
  - DB2
datasource:
  keys:
    - type
    - host
    - port
  values:
    type: db2
datasets:
  - name: Table
    identification:
      keys:
        - catalog
        - schema
        - table
