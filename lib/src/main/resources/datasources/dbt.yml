connectorIds:
  - dbt-cloud
  - dbt
datasource:
  keys:
    - type
  values:
    type: dbt
dataProcesses:
  - name: job
    identification:
      keys:
        - project
        - unique_id
datasets: []