datasource:
  keys:
    - type
    - region
    - account
  values:
    type: glue-datacatalog
datasets:
  - name: Dataset
    identification:
      keys:
        - catalog
        - database
        - table
      fields:
        - field_key