connectorIds:
  - BigQuery
  - BigQueryOrganization
datasource:
  keys:
    - type
  values:
    type: bigquery
datasets:
  - name: Dataset
    identification:
      keys:
        - project
        - dataset
        - table
