connectorIds:
  - bigquery-v2
datasource:
  keys:
    - type
  values:
    type: bigquery
datasets:
  - name: Dataset
    identification:
      keys:
        - project
        - dataset
        - table
    fields:
      - field_key
