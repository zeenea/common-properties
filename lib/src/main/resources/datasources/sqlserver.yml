connectorIds:
  - SQLServer
datasource:
  keys:
    - type
    - host
    - port
  values:
    type: sqlserver
datasets:
  - name: Table
    identification:
      keys:
        - catalog
        - schema
        - table
    fields:
      - field_key
