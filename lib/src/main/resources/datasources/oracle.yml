connectorIds:
  - Oracle
datasource:
  keys:
    - type
    - host
    - port
  values:
    type: oracle
datasets:
  - name: Table
    identification:
      keys:
        - database
        - schema
        - table
