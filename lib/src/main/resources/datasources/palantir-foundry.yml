datasource:
  keys:
    - type
    - host
  values:
    type: palantir-foundry

datasets:
  - name: Dataset
    identification:
      keys:
        - rid
    fields:
      - field_key
    label:
      keys:
        - rid
        - prefix
