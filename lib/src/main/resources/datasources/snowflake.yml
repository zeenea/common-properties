connectorIds:
  - snowflake-v2

datasource:
  keys:
    - type
    - account_id
  values:
    type: snowflake

datasets:
  - name: Table
    identification:
      keys:
        - catalog
        - schema
        - table
    fields:
      - name

dataProcesses:
  - name: PipeLineage
    identification:
      keys:
        - pipe_marker
        - catalog
        - schema
        - pipe
  - name: HistoryLineage
    identification:
      keys:
        - queryType
        - hash