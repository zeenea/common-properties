connectorIds:
  - snowflake-v2
datasource:
  keys:
    - type
    - account
  values:
    type: snowflake
datasets:
  - name: Table
    identification:
      keys:
        - catalog
        - schema
        - table
