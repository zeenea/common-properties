connectorIds:
  - snowflake-v2

datasource:
  keys:
    - type
    - account_id
  values:
    type: snowflake

datasets:
  - name: Table
    identification:
      keys:
        - catalog
        - schema
        - table
    fields:
      - name

dataProcesses:
  - name: PipeLineage
    identification:
      keys:
        - pipe_marker
        - catalog
        - schema
        - pipe
      values:
        pipe_marker: pipe
  - name: HistoryLineage
    identification:
      keys:
        - query_type
        - hash