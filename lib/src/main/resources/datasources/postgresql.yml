datasource:
  keys:
    - type
    - host
    - port
  values:
    type: postgres
datasets:
  - name: Table
    identification:
      keys:
        - catalog
        - schema
        - table
    fields:
      - field_key
